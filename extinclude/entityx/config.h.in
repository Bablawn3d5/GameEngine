#pragma once

#include <cstdint>
#include <cstddef>
#include <chrono>

namespace entityx {

static const size_t MAX_COMPONENTS = @ENTITYX_MAX_COMPONENTS@;
typedef std::chrono::duration<float, std::chrono::seconds::period> TimeDelta;

}  // namespace entityx
